# Story 5.2: Enhanced Citation Footer Component

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a developer,
I want the raw source metadata from the LLM response to be beautifully formatted as a dedicated footer block,
so that I can easily scan and click the exact "proof" links verifying the AI's answer.

## Acceptance Criteria

1. **Given** the raw citation list output from Story 4.5
2. **When** the `ui/citations.py` helper function is called
3. **Then** it renders a visual markdown separator (`---`) at the bottom of the chat bubble
4. **And** it renders a bulleted list of citation links using sub-text styling (`font-size:0.8em; color:gray;`)
5. **And** the links are descriptive (e.g., using the `title` metadata rather than raw URLs)
6. **And** clicking a link opens the target in a new browser tab (`target="_blank"`)

## Tasks / Subtasks

- [ ] Task 1: Create Citation Helper Module
  - [ ] Create `ui/citations.py`.
  - [ ] Implement a function (e.g., `format_citation_footer(docs) -> str`) that accepts LangChain `Document` objects.
  - [ ] Ensure the function deduplicates chunks from the same page using the `url` metadata field.
- [ ] Task 2: Implement HTML/Markdown Styling
  - [ ] Begin the returned string with `\n\n---\n**Sources:**\n`.
  - [ ] Wrap the citation list in an HTML block with sub-text styling: `<div style="font-size: 0.8em; color: gray;">`.
  - [ ] Use raw HTML `<a>` tags instead of markdown links to enforce `target="_blank"`. Example: `<a href="{url}" target="_blank">{title}</a>`.
- [ ] Task 3: Integrate with Chat UI
  - [ ] In `ui/app.py`, replace the raw citation building logic from 4.5 with a call to `format_citation_footer`.
  - [ ] Ensure that when the citation string is rendered or appended to session state, `st.markdown(..., unsafe_allow_html=True)` is used, otherwise the HTML tags will render as raw text.

## Dev Notes

- **Streamlit Link Targeting:** Standard markdown links `[text](url)` in Streamlit do not consistently open in new tabs depending on the browser environment. Using explicit HTML `<a target="_blank">` is mandatory to satisfy AC 6 and prevent the user from losing their chat state.
- **Security:** Using `unsafe_allow_html=True` is safe here because the URLs and Titles are strictly generated by our internal ingestion pipeline, not user input.
- **Architectural Constraints:** Keep this logic out of `ui/chain.py`. The chain should only return raw objects; `ui/citations.py` handles the Streamlit-specific HTML formatting.

### Project Structure Notes

- Creates `ui/citations.py` (if not already created as a placeholder in 4.5).
- Updates `ui/app.py`.

### References

- [Source: epics.md#Story 5.2: Enhanced Citation Footer Component]
- [Source: ux-design-specification.md#3. Custom Component Specifications]

## Dev Agent Record

### Agent Model Used

Gemini 3.1 Pro (High)

### Debug Log References

### Completion Notes List

- Detailed the technical constraint around Streamlit markdown links vs HTML anchor tags.
- Solidified the architecture pattern of using dedicated UI helper modules.

### File List

- `ui/citations.py` (to be created)
- `ui/app.py` (to be updated)
