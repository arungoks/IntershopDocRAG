# tests/conftest.py
# Shared pytest fixtures and configuration for the IntershopRAG test suite.
# Add project-wide fixtures here as the test suite grows.

import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent.parent))
